#ifndef __tavival_H__
#define __tavival_H__

#include <stdlib.h>
#include <time.h>

#include <vector>
#include <string>
#include <map>
#include <set>
#include <list>
#include <mutex>
#include <unordered_map>
#include <unordered_set>

#include <iostream>
#include <memory>
#include <fstream>
#include <sstream>

using std::vector;
using std::string;
using std::pair;
using std::map;
using std::set;
using std::list;
using std::unordered_map;
using std::unordered_set;

using std::cout;
using std::shared_ptr;
using std::ifstream;
using std::ofstream;
using std::istringstream;
using std::mutex;
using std::lock_guard;

/* struct TLV{ */
/*     int type; */
/*     int len; */
/*     vector<char> value; */  
/* }; */
/* enum{Recomand,Search}; */

#endif 

